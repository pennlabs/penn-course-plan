const sectionList = (state = {}, sections) => (
    {
        ... state,
        sections
    }
);

const sectionInfo = (state = {}, sectionInfo) => (
    {
        ... state,
        sectionInfo
    }
);